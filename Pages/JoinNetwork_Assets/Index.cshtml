@page
@model tbkk_AC.Pages.JoinNetwork_Assets.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Join_Network_Asset[0].Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_Network_Asset[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_Network_Asset[0].Network)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_Network_Asset[0].Asset)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Join_Network_Asset) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Network.IpAddr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asset.AssetName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.JoinNetAsID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.JoinNetAsID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.JoinNetAsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
