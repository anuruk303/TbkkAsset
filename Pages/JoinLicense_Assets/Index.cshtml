@page
@model tbkk_AC.Pages.JoinLicense_Assets.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Join_License_Asset[0].Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_License_Asset[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_License_Asset[0].License)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_License_Asset[0].Asset_AssetID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Join_License_Asset) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.License.Attachfiles)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asset_AssetID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.JoinLiAsID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.JoinLiAsID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.JoinLiAsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
