@page
@model tbkk_AC.Pages.JoinAsset_Emp.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Join_Asset_Emp[0].Note)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_Asset_Emp[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_Asset_Emp[0].Employee_EmployeeID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Join_Asset_Emp[0].Asset)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Join_Asset_Emp) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Note)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee_EmployeeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asset.AssetName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.JoinAsEmpID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.JoinAsEmpID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.JoinAsEmpID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
